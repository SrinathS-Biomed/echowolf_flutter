name: echowolf
description: EchoWolf OBD-II Diagnostic Suite

publish_to: 'none'
version: 4.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.31.7
  provider: ^6.1.1
  fl_chart: ^0.68.0
  permission_handler: ^11.3.0
  shared_preferences: ^2.2.2
  csv: ^6.0.0
  path_provider: ^2.1.2
  intl: ^0.19.0
  wakelock_plus: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
